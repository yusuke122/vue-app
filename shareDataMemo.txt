メインマップ
最初だけ中心座標とズームレベルがわかっていればいい
onbeforemountで
apiで取得できてたら親側で更新してから渡す
<Child :data="type?.value?.cycle ?? config" />
マップインスタンスは引き続きリアクティブ
リアクティブでなくてもできるならそうするべきだがとりあえず。
その後は中心座標が変わる時、ズームレベルが変わる時、クリックイベントが発生したきときにエミット

表示選択
コンフィグの静的な表示項目だけわかっていればいい
あとはエミット
コントロールのバリューに
デフォは0,2にして0,1,2,3をしかけておく
地誌モードに0,1を仕掛けておく
これを投げる、親側でタイプを判定

凡例
表示タイプをリアクティブで受け取る

テーブル
データをリアクティブで受け取る
データはタイプに紐づく生データだけ、全部渡さない
mapViewRawDataだけもらう
受け取っとものを表示するだけ

地図選択
コンフィグの更新周期だけ静的にうけとる
あとはエミット

親が管理するデータ群をまとめる必要はない

描画更新
APi起動周期、更新タイミングそれぞれコンフィグからとる、
セットインターバル
描画更新はupdategeo7/8/9
モードでどのアップデートか判断
みえているレイヤだけ描き直す

updateGeo各種
0〜4
地図、角コントロール
addcontrol
5:ポップアップ 表示非表示も
addoverlay
6:フィーチャー　ポイント
 displaymodeからスタイル決定
7:フィーチャー　ポリゴン
　displaymodeからスタイル決定
8:フィーチャー　サークル
　displaymodeからスタイル決定
addfeature

changegeo
0 表示選択更新
displaymodeかgeomodeが送られてくる
(オブジェクトでモードとナム)
どっちかのモードを更新後、スイッチで表示タイプの更新
表示タイプ更新後、スイッチでテーブルに渡す用の生データを更新
これでコントロール更新が完了する。
次にスイッチでupdateGeo7/8/9どれかを起動
updateGeo後に表示非表示を切り替える
1 グループ更新
受け取ったグループ番号で更新
対象レイヤーはdisplayTypeと同じ
radioID、layerIDで判断
（無線のみはradioID1だけ　
分岐を減らす目的で何もないlayerID1もつくる）
描画中のフィーチャーのgroupIDをみて表示を切り替え
2 ベース地図切り替え
mapIDをみて切り替え
